import { SectionLoading } from "@/components/app/section-loading";

export default function AdminLoading() {
  return (
    <SectionLoading
      title="Loading the admin workspace"
      description="Preparing operations metrics, moderation queues, and management surfaces."
    />
  );
}
