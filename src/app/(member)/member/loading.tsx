import { SectionLoading } from "@/components/app/section-loading";

export default function MemberLoading() {
  return (
    <SectionLoading
      title="正在载入会员工作台"
      description="正在准备任务、奖励、素材历史与 AI 礼宾内容。"
    />
  );
}
