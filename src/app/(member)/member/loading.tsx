import { SectionLoading } from "@/components/app/section-loading";

export default function MemberLoading() {
  return (
    <SectionLoading
      title="Loading your member workspace"
      description="Preparing missions, rewards, asset history, and concierge guidance."
    />
  );
}
